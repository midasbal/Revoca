import { PlaceholderPage } from './PlaceholderPage';

export default function LendPage() {
  return (
    <PlaceholderPage
      eyebrow="Lend"
      title="Post collateral, borrow against your standing"
      description="Tier-scaled borrowing lands here: how much you can borrow follows directly from your attested tier, read live from the registry."
    />
  );
}
