import { PlaceholderPage } from './PlaceholderPage';

export default function DocsPage() {
  return (
    <PlaceholderPage
      eyebrow="Docs"
      title="How Revoca actually enforces compliance"
      description="The architecture write-up, the compliance gate design, and the unwind lifecycle, in plain language, land here next."
    />
  );
}
